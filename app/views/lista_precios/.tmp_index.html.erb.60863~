<h1>Listing lista_precios</h1>

<table>
  <tr>
    <th>File</th>
    <th>Password</th>
    <th>Desde</th>
    <th>Hasta</th>
  </tr>

<% @lista_precios.each do |lista_precio| %>
  <tr>
    <td><%=h lista_precio.file %></td>
    <td><%=h lista_precio.password %></td>
    <td><%=h lista_precio.desde %></td>
    <td><%=h lista_precio.hasta %></td>
    <td><%= link_to 'Show', lista_precio %></td>
    <td><%= link_to 'Edit', edit_lista_precio_path(lista_precio) %></td>
    <td><%= link_to 'Destroy', lista_precio, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New lista_precio', new_lista_precio_path %>